// GENERATED by theme/apply.py — {{ name_title }}
{
  "$schema": "https://github.com/fastfetch-cli/fastfetch/raw/dev/doc/json_schema.json",
  "logo": { "padding": { "top": 1, "left": 2 } },
  "display": { "color": { "keys": "{{ color.magenta }}", "title": "{{ color.peach }}" } },
  "modules": [
    { "type": "title", "keyColor": "{{ color.magenta }}", "format": "{user-name}㉿{host-name}" },
    "separator",
    { "type": "os",        "key": " OS  ", "keyColor": "{{ color.magenta }}" },
    { "type": "kernel",    "key": " KRN ", "keyColor": "{{ color.magenta }}" },
    { "type": "uptime",    "key": "󰅐 UPT ", "keyColor": "{{ color.magenta }}" },
    { "type": "packages",  "key": " PKG ", "keyColor": "{{ color.magenta }}" },
    { "type": "shell",     "key": " SH  ", "keyColor": "{{ color.magenta }}" },
    { "type": "wm",        "key": " WM  ", "keyColor": "{{ color.magenta }}" },
    { "type": "terminal",  "key": " TTY ", "keyColor": "{{ color.magenta }}" },
    { "type": "cpu",       "key": " CPU ", "keyColor": "{{ color.peach }}" },
    { "type": "gpu",       "key": "󰢮 GPU ", "keyColor": "{{ color.peach }}" },
    { "type": "memory",    "key": " MEM ", "keyColor": "{{ color.peach }}" },
    { "type": "disk",      "key": "󰋊 DSK ", "keyColor": "{{ color.peach }}" },
    { "type": "battery",   "key": "󰁹 BAT ", "keyColor": "{{ color.peach }}" },
    { "type": "localip",   "key": "󰩟 NET ", "keyColor": "{{ color.peach }}" },
    "break",
    { "type": "colors", "paddingLeft": 2, "symbol": "circle" }
  ]
}
