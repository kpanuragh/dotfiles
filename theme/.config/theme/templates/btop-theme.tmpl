# GENERATED by theme/apply.py — {{ name_title }}
theme[main_bg]="{{ color.base }}"
theme[main_fg]="{{ color.text }}"
theme[title]="{{ color.text }}"
theme[hi_fg]="{{ color.magenta }}"
theme[selected_bg]="{{ color.surface1 }}"
theme[selected_fg]="{{ color.magenta }}"
theme[inactive_fg]="{{ color.muted }}"
theme[graph_text]="{{ color.subtext }}"
theme[meter_bg]="{{ color.surface0 }}"
theme[proc_misc]="{{ color.peach }}"
theme[cpu_box]="{{ color.violet_deco }}"
theme[mem_box]="{{ color.violet_deco }}"
theme[net_box]="{{ color.violet_deco }}"
theme[proc_box]="{{ color.violet_deco }}"
theme[div_line]="{{ color.surface2 }}"
theme[temp_start]="{{ color.green }}"
theme[temp_mid]="{{ color.yellow }}"
theme[temp_end]="{{ color.red }}"
theme[cpu_start]="{{ color.cyan }}"
theme[cpu_mid]="{{ color.magenta }}"
theme[cpu_end]="{{ color.peach }}"
theme[free_start]="{{ color.surface2 }}"
theme[free_mid]="{{ color.blue }}"
theme[free_end]="{{ color.cyan }}"
theme[cached_start]="{{ color.blue }}"
theme[cached_mid]="{{ color.violet }}"
theme[cached_end]="{{ color.magenta }}"
theme[available_start]="{{ color.yellow }}"
theme[available_mid]="{{ color.peach }}"
theme[available_end]="{{ color.red }}"
theme[used_start]="{{ color.green }}"
theme[used_mid]="{{ color.yellow }}"
theme[used_end]="{{ color.red }}"
theme[download_start]="{{ color.cyan }}"
theme[download_mid]="{{ color.blue }}"
theme[download_end]="{{ color.violet }}"
theme[upload_start]="{{ color.magenta }}"
theme[upload_mid]="{{ color.peach }}"
theme[upload_end]="{{ color.yellow }}"
theme[process_start]="{{ color.violet }}"
theme[process_mid]="{{ color.magenta }}"
theme[process_end]="{{ color.peach }}"
