-- GENERATED by theme/apply.py — do not edit.
-- Edit theme/palettes/{{ name }}.toml instead.
--
-- Pure data: this file returns a table and calls no Hyprland API, so it
-- cannot break the compositor config beyond the values it supplies.
return {
	gaps_in = {{ geometry.gaps_in }},
	gaps_out = {{ geometry.gaps_out }},
	border_size = {{ geometry.border_size }},

	active_border_1 = "rgba({{ color.magenta_bare }}ee)",
	active_border_2 = "rgba({{ color.peach_bare }}ee)",
	inactive_border = "rgba({{ color.surface2_bare }}aa)",

	rounding = {{ geometry.rounding }},
	dim_inactive = {{ geometry.dim_inactive }},
	dim_strength = {{ geometry.dim_strength }},
	active_opacity = {{ geometry.active_opacity }},
	inactive_opacity = {{ geometry.inactive_opacity }},

	blur_size = {{ geometry.blur_size }},
	blur_passes = {{ geometry.blur_passes }},
	blur_vibrancy = {{ geometry.blur_vibrancy }},

	shadow_color = 0xee{{ color.mantle_bare }},
}
