;; -*- no-byte-compile: t; -*-
;;; packages.el

;; Extra packages on top of what Doom modules pull in.

(package! super-save)

;; ACP-based AI agent shell. Pulls in acp.el + shell-maker from MELPA.
(package! agent-shell)
